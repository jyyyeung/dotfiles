

if [[ "$(uname)" == 'Darwin' ]]; then
  fpath=( "$HOME/Library/Python/3.9/lib/python/site-packages/argcomplete/bash_completion.d" "${fpath[@]}" )
fi

if [[ "$(uname)" == 'Linux' ]]; then
  fpath=( "$HOME/.local/lib/python3.10/site-packages/argcomplete/bash_completion.d" "${fpath[@]}" )
fi

